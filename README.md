# proxchunk
Fully automatic multi-proxy Range chunked Linux CLI downloader in modern C++23. Fetches/updates proxy lists, drops dead, sorts by speed, parallel chunk downloads via different IPs, assembles file. Bypasses per-IP throttling on filesharing sites.
